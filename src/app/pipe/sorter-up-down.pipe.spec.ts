import { SorterUpDownPipe } from './sorter-up-down.pipe';

describe('SorterUpDownPipe', () => {
  it('create an instance', () => {
    const pipe = new SorterUpDownPipe();
    expect(pipe).toBeTruthy();
  });
});
